<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="picking_price_decimal.list">
      <field name="name">picking_price_decimal list</field>
      <field name="model">picking_price_decimal.picking_price_decimal</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="picking_price_decimal.action_window">
      <field name="name">picking_price_decimal window</field>
      <field name="res_model">picking_price_decimal.picking_price_decimal</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="picking_price_decimal.action_server">
      <field name="name">picking_price_decimal server</field>
      <field name="model_id" ref="model_picking_price_decimal_picking_price_decimal"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="picking_price_decimal" id="picking_price_decimal.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="picking_price_decimal.menu_1" parent="picking_price_decimal.menu_root"/>
    <menuitem name="Menu 2" id="picking_price_decimal.menu_2" parent="picking_price_decimal.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="picking_price_decimal.menu_1_list" parent="picking_price_decimal.menu_1"
              action="picking_price_decimal.action_window"/>
    <menuitem name="Server to list" id="picking_price_decimal" parent="picking_price_decimal.menu_2"
              action="picking_price_decimal.action_server"/>
    -->
  </data>
</odoo>
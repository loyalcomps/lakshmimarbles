<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="decimal_points.list">
      <field name="name">decimal_points list</field>
      <field name="model">decimal_points.decimal_points</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="decimal_points.action_window">
      <field name="name">decimal_points window</field>
      <field name="res_model">decimal_points.decimal_points</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="decimal_points.action_server">
      <field name="name">decimal_points server</field>
      <field name="model_id" ref="model_decimal_points_decimal_points"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="decimal_points" id="decimal_points.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="decimal_points.menu_1" parent="decimal_points.menu_root"/>
    <menuitem name="Menu 2" id="decimal_points.menu_2" parent="decimal_points.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="decimal_points.menu_1_list" parent="decimal_points.menu_1"
              action="decimal_points.action_window"/>
    <menuitem name="Server to list" id="decimal_points" parent="decimal_points.menu_2"
              action="decimal_points.action_server"/>
    -->
  </data>
</odoo>
<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="pos_auto_invoice.list">
      <field name="name">pos_auto_invoice list</field>
      <field name="model">pos_auto_invoice.pos_auto_invoice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="pos_auto_invoice.action_window">
      <field name="name">pos_auto_invoice window</field>
      <field name="res_model">pos_auto_invoice.pos_auto_invoice</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="pos_auto_invoice.action_server">
      <field name="name">pos_auto_invoice server</field>
      <field name="model_id" ref="model_pos_auto_invoice_pos_auto_invoice"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="pos_auto_invoice" id="pos_auto_invoice.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="pos_auto_invoice.menu_1" parent="pos_auto_invoice.menu_root"/>
    <menuitem name="Menu 2" id="pos_auto_invoice.menu_2" parent="pos_auto_invoice.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="pos_auto_invoice.menu_1_list" parent="pos_auto_invoice.menu_1"
              action="pos_auto_invoice.action_window"/>
    <menuitem name="Server to list" id="pos_auto_invoice" parent="pos_auto_invoice.menu_2"
              action="pos_auto_invoice.action_server"/>
    -->
  </data>
</odoo>
<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="pos_balance_correction.list">
      <field name="name">pos_balance_correction list</field>
      <field name="model">pos_balance_correction.pos_balance_correction</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="pos_balance_correction.action_window">
      <field name="name">pos_balance_correction window</field>
      <field name="res_model">pos_balance_correction.pos_balance_correction</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="pos_balance_correction.action_server">
      <field name="name">pos_balance_correction server</field>
      <field name="model_id" ref="model_pos_balance_correction_pos_balance_correction"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="pos_balance_correction" id="pos_balance_correction.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="pos_balance_correction.menu_1" parent="pos_balance_correction.menu_root"/>
    <menuitem name="Menu 2" id="pos_balance_correction.menu_2" parent="pos_balance_correction.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="pos_balance_correction.menu_1_list" parent="pos_balance_correction.menu_1"
              action="pos_balance_correction.action_window"/>
    <menuitem name="Server to list" id="pos_balance_correction" parent="pos_balance_correction.menu_2"
              action="pos_balance_correction.action_server"/>
    -->
  </data>
</odoo>
<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="avoid_brand_dupli.list">
      <field name="name">avoid_brand_dupli list</field>
      <field name="model">avoid_brand_dupli.avoid_brand_dupli</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="avoid_brand_dupli.action_window">
      <field name="name">avoid_brand_dupli window</field>
      <field name="res_model">avoid_brand_dupli.avoid_brand_dupli</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="avoid_brand_dupli.action_server">
      <field name="name">avoid_brand_dupli server</field>
      <field name="model_id" ref="model_avoid_brand_dupli_avoid_brand_dupli"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="avoid_brand_dupli" id="avoid_brand_dupli.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="avoid_brand_dupli.menu_1" parent="avoid_brand_dupli.menu_root"/>
    <menuitem name="Menu 2" id="avoid_brand_dupli.menu_2" parent="avoid_brand_dupli.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="avoid_brand_dupli.menu_1_list" parent="avoid_brand_dupli.menu_1"
              action="avoid_brand_dupli.action_window"/>
    <menuitem name="Server to list" id="avoid_brand_dupli" parent="avoid_brand_dupli.menu_2"
              action="avoid_brand_dupli.action_server"/>
    -->
  </data>
</odoo>
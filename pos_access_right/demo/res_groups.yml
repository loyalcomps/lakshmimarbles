# -*- coding: utf-8 -*-

- !record {model: res.groups, id: group_discount}:
    users:
        - base.user_root
        - base.user_demo

- !record {model: res.groups, id: group_change_unit_price}:
    users:
        - base.user_root

- !record {model: res.groups, id: group_cash_discount}:
    users:
        - base.user_root

- !record {model: res.groups, id: group_pos_discount}:
    users:
        - base.user_root
